class pMHC_type:
    def __init__(self, resi, pos, entity):
        self.resi = resi  # Residue types
        self.pos = pos  # Positions (coordinates)
        self.entity = entity  # Entity information