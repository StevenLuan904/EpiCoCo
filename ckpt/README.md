This folder is intentionally empty for the GitHub release.

Place your checkpoints here and update `configs/sample/sample_pmhc.yml` to point to them.

Example layout:

```bash
ckpt
├── epicoco.pt
```
